{
  "name": "vivere-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for Vivere, a TypeScript framework for building Discord bots.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20.11" },
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "eslint": "^9.12.0",
    "typescript-eslint": "^8.8.0",
    "prettier": "^3.3.0"
  }
}
